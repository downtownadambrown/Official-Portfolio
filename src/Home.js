import React, {useRef, useState, useEffect} from 'react';
import './Home.css';

const Home = () => {
    return (
        <React.Fragment>
            <div>
                This is a home page
            </div>
        </React.Fragment>
    );
};

export default Home;
